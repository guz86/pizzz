<h2>Welcome to Candy shop</h2>


<div class="row">
	<% @products.each do |p| %>
	<div class="col-md-3">
		<%= p.title %>
	</div>
	<% end %>
</div>

<button class="btn btn-primary" onClick="something()">Click me!</button>
